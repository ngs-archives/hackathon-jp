<?xml version="1.0" encoding="UTF-8"?>
<Module>
  <ModulePrefs title="Gifts part 1 - Friends">
    <Require feature="opensocial-0.7"/>
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
      <script type="text/javascript">
        gadgets.util.registerOnLoadHandler(init);

var list = [];

function init() {
	request();
}

function request() {
	var req = opensocial.newDataRequest();
	req.add(req.newFetchPersonRequest('VIEWER'), 'viewer');
	req.add(req.newFetchPeopleRequest('VIEWER_FRIENDS'), 'viewerFriends');
	req.send(response);
}


function response(data) {
  var viewer = data.get("viewer").getData();
  //var name = viewer.getDisplayName();
  //var thumb = viewer.getField(opensocial.Person.Field.THUMBNAIL_URL);
  var viewerFriends = data.get('viewerFriends').getData();
  
    
  html = new Array();
  html.push('<table cellpadding="5"><tr>');
  viewerFriends.each(function(person) {
	param = viewer.getDisplayName() + "," + viewer.getId() + ",test" + person.getDisplayName() + ',' + person.getId() + ",test";	
	alert(param);
	var thumb = viewer.getField(opensocial.Person.Field.THUMBNAIL_URL);
    html.push('<td>' + '<a href="#" onClick="startGame(' + param + ');">' + '<img src="' + thumb + '">' + '</a></td>');
  });
  
  html.push('</tr><tr>');
  
  viewerFriends.each(function(person) {
	param = viewer.getDisplayName() + "," + viewer.getId() + ",test" + person.getDisplayName() + ',' + person.getId() + ",test";
	
	var thumb = viewer.getField(opensocial.Person.Field.THUMBNAIL_URL);
    html.push('<td>' + '<a href="#" onClick="startGame(' + param + ');">' + person.getDisplayName() + '</a></td>');
  });
  
  html.push('</tr></table>');
  document.getElementById('friends').innerHTML = html.join('');
}

function startGame(name0, id0, thumb0, name1, id1, thumb1) {
	alert(name0);
}

	  </script>
	  <div id="friends"></div>
   ]]>
  </Content>
</Module>
