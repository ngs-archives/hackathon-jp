<h1>Listing prefectures</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Url</th>
  </tr>

<% for prefecture in @prefectures %>
  <tr>
    <td><%=h prefecture.name %></td>
    <td><%=h prefecture.url %></td>
    <td><%= link_to 'Show', prefecture %></td>
    <td><%= link_to 'Edit', edit_prefecture_path(prefecture) %></td>
    <td><%= link_to 'Destroy', prefecture, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New prefecture', new_prefecture_path %>
