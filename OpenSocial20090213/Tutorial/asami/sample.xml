<?xml version="1.0" encoding="UTF-8"?>
<Module>
  <ModulePrefs title="sample">
    <Require feature="opensocial-0.8"/>
    <Require feature="views" />
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
 	<script>
		
		var defaultItemList = ['ごはん', 'しお', 'ミソ', '小麦粉'];
		
		gadgets.util.registerOnLoadHandler(init);

		function init() {
		  onLoadItems();
		}
		
		function onLoadItems() {

		  html = new Array();
		  html.push('</ul>');
		  html.push('<select id="item_select">');
		  
		  for(i = 0 ; i < defaultItemList.length ; i++) {
			html.push('<option value="', person.getId(), '">', person.getDisplayName(), '</option>');
		  }
          html.push('</select>');

		  document.getElementById('items_a').innerHTML = html.join('');
		  document.getElementById('items_b').innerHTML = html.join('');
		}
		
		
	</script>
	
	<div id='main'>
	   items:
	   <div id='items_a'></div> + <div id='items_b'></div> =
	</div>
    ]]>
  </Content>
</Module>
